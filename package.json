{
  "name": "@organic-ui/monorepo",
  "private": true,
  "scripts": {
    "docs": "pnpm --filter docs dev",
    "build": "pnpm --filter organic-ui build",
    "publish": "pnpm --filter organic-ui publish --access public"
  }
}